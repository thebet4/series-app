import { LightText } from '@theme/Fonts'
import styled from 'styled-components/native'

export const Container = styled.View`
  width: 64px;
  flex-direction: row;
`
export const AverageText = styled(LightText)`
  font-size: 16px;
  line-height: 24px;
  margin-left: 8px;
`
